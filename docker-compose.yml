version: "3.9"

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sense_ai
    ports:
      - "7070:7070"
    volumes:
      - ./src/:/api/src
    restart: unless-stopped
